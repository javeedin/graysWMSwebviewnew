# Works on Python 3.14+ (no Rust / C++ compilation required)
# Install: pip install --prefer-binary -r requirements.txt
# Run:     python rag_service.py

# Web server — pure Python, no Rust
flask==3.1.0
flask-cors==5.0.0

# Vector DB
chromadb==0.5.3

# Embeddings (downloads ~80MB model on first run, then fully offline)
sentence-transformers==3.0.1

# Claude AI
anthropic==0.40.0

# HTTP
requests==2.32.3
